# ROI 存視窗相對比例，不存螢幕絕對座標

絕對座標在視窗移動／換解析度／重開遊戲後就失效，是「每次重開都要重框」的主因。改存客戶區比例後，幾何變化自動跟著走；舊絕對座標在首次綁定成功時自動遷移（之後刪除舊鍵）。管線內部仍用絕對座標計算，由綁定層每 tick 換算，Solver 以下完全不受影響。

## Considered Options

- **絕對座標＋啟動吸附**：曾實作過（附近找→全螢幕找），但治標不治本，且全螢幕搜尋有誤認其他格子狀視窗的風險，改為比例制後吸附只剩遷移輔助。
- **每次啟動強制重框**：零歧義但使用者拒絕（本 issue 的起點）。
